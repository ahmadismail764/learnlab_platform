services:
  db:
    image: postgres:14
    restart: always
    environment:
      POSTGRES_PASSWORD: SoftwareSQL11__
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
 
